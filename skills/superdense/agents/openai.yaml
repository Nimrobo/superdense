interface:
  display_name: "Superdense Stored Sessions"
  short_description: "Find and read prior AI-agent sessions"
  default_prompt: "Use $superdense to find and read relevant prior AI-agent sessions, explain what happened, compare attempts, audit errors or plan-mode behavior, and avoid exposing raw records unless asked."
