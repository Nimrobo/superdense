interface:
  display_name: "Road42 Stored Sessions"
  short_description: "Recover context from saved agent sessions"
  default_prompt: "Use $road42 to find relevant prior agent sessions and explain what happened without exposing raw records unless asked."
